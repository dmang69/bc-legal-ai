global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Wire when FastAPI /metrics is exposed
  # - job_name: ala-api
  #   static_configs:
  #     - targets: ["host.docker.internal:8000"]
