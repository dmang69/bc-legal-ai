# Shared packages (target monorepo)

| Package | Role |
|---------|------|
| `api-client/` | Typed HTTP client for private backend |
| `shared-types/` | Matter, evidence, consent DTOs |
| `design-system/` | UI primitives for Workbench / Client / Portal |
| `legal-viewer/` | Document / evidence presentation |
| `validation/` | Shared Zod/schema validation |

Scaffold packages as work progresses (M6A-003 / M6A-004). Until then, types may live under `apps/platform-ui/src`.
