# Copy to .env (gitignored). Never commit real secrets or client matter IDs.

# Hugging Face (write token for Space deploy only — rotate if exposed)
# HF_TOKEN=

# Postgres (from infra/docker-compose)
ALA_POSTGRES_URL=postgresql://ala:change_me@localhost:5432/ala

# Public demo: never enable real uploads of confidential files
PUBLIC_DEMO=1
ALLOW_CLIENT_UPLOADS=0
