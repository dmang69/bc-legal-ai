# Matter isolation

Every request is scoped to a `matter_id`. Evidence matrix rows, drafts, and chat history never cross matters.

See `architecture/ALA_SYSTEM.md` and `Matter` in `architecture/schemas.py`.
