{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Minimal capabilities for BC Legal AI shell — expand only with security review",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
