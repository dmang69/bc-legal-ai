# Tauri icons

Place platform icons here (or generate with `npm run tauri icon path/to/1024.png`).

For prototype builds, copy from `frontend/client/icons/icon-512.png` and run the Tauri icon generator so Windows/macOS/Android/iOS get correctly sized assets.
