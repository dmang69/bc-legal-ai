{
  "name": "@bc-legal-ai/desktop-mobile",
  "version": "0.1.2",
  "private": true,
  "description": "Tauri 2 shell — BC Legal AI Workbench / Client (Windows, macOS, Android, iOS)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:windows": "tauri build --config src-tauri/tauri.windows.conf.json",
    "build:macos": "tauri build --config src-tauri/tauri.macos.conf.json",
    "android:init": "tauri android init",
    "ios:init": "tauri ios init",
    "android:dev": "tauri android dev",
    "ios:dev": "tauri ios dev",
    "verify:config": "node ./scripts/verify-upgrade-config.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-process": "^2.0.0",
    "@tauri-apps/plugin-updater": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
