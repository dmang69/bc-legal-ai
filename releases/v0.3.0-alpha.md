# BC Legal AI Associate — v0.3.0-alpha

**Release date:** 2026-07-28  
**Git tag:** `v0.3.0-alpha`  
**Classification:** Internal alpha foundations for six production tracks — **not** production

## Tracks landed

1. **Postgres multi-worker** — dialect-compatible SQL (`?`→`%s`), optional `psycopg_pool`, health reporting  
2. **Cookie sessions** — HttpOnly `ala_session` + CSRF; Bearer still supported  
3. **OCR path** — pypdf native extract + optional Tesseract; pages marked `needs_ocr` when empty  
4. **Official-law retrieval** — BC Laws host-allowlisted fetch + currency line; never auto court-ready  
5. **Court-ready export** — ZIP (DOCX summary + manifest) only after APPROVED export manifest  
6. **Signed installers** — `sign_windows_installer.ps1` + signing policy doc (certs human-gated)

Full detail: [`docs/PRODUCTION_TRACKS_V03.md`](../docs/PRODUCTION_TRACKS_V03.md)

## Gates

- pytest green (218+ tests at cut)
- confidential scan OK
- public-demo deployment readiness OK

## Not in this release

- Live Postgres load testing / RLS
- Production TLS edge + Secure cookie force on all hosts
- System Tesseract/Poppler packaging for all deploy targets
- Full form-faithful court PDFs / BOA automation
- Organization code-signing certificate or Apple notarization
