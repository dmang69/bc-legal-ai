# Client portal frontend (Layer 5)

Scaffold only. Production client UI will use MFA + matter ACL.

**Do not** deploy this public-facing with real litigation files until auth,
encryption, and privilege guards are live.

Suggested stack later: React/Vite or Next.js against `services/client_portal`
and `services/messaging`.

Static placeholder: `index.html` (no backend).
